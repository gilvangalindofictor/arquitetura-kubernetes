# =============================================================================
# entities/domains/catalog-info.yaml — 5 Domain entities da plataforma
# Mapeamento: domínio → team owner (ADR-047)
# Nomes canônicos: platform, integration, data, operations, shared-services
#
# Sprint S6-A / M2 — ADR-055, ADR-102, ADR-047
# =============================================================================

# ---------------------------------------------------------------------------
# Domínio 1: platform
# Owner: platform-team
# Sistemas: backstage, argocd, keycloak, harbor, vault
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: platform
  title: "Platform"
  description: "Infraestrutura de plataforma — IDP, GitOps, Auth, Registry e Secrets Management"
  annotations:
    backstage.io/managed-by-location: "url:https://gitlab.staging.internal/platform/backstage-catalog/-/blob/main/entities/domains/catalog-info.yaml"
    backstage.io/techdocs-ref: "dir:."
  tags:
    - platform
    - idp
    - gitops
    - auth
    - secrets
spec:
  owner: platform-team

---
# ---------------------------------------------------------------------------
# Domínio 2: integration
# Owner: integration-team
# Sistemas: api-gateway, event-bus, webhooks, message-broker
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: integration
  title: "Integration"
  description: "Camada de integração — API Gateway, Event Bus, Webhooks e Message Broker"
  annotations:
    backstage.io/managed-by-location: "url:https://gitlab.staging.internal/platform/backstage-catalog/-/blob/main/entities/domains/catalog-info.yaml"
    backstage.io/techdocs-ref: "dir:."
  tags:
    - integration
    - api-gateway
    - event-bus
    - webhooks
    - rabbitmq
spec:
  owner: integration-team

---
# ---------------------------------------------------------------------------
# Domínio 3: data
# Owner: data-team
# Sistemas: data-hatch-etl, data-pipelines, data-warehouse
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: data
  title: "Data"
  description: "Serviços de dados — pipelines ETL, ingestão, transformação e entrega de dados"
  annotations:
    backstage.io/managed-by-location: "url:https://gitlab.staging.internal/platform/backstage-catalog/-/blob/main/entities/domains/catalog-info.yaml"
    backstage.io/techdocs-ref: "dir:."
  tags:
    - data
    - etl
    - pipeline
    - python
    - data-warehouse
spec:
  owner: data-team

---
# ---------------------------------------------------------------------------
# Domínio 4: operations
# Owner: operations-team
# Sistemas: prometheus-stack, loki, tempo, grafana, alertmanager, velero
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: operations
  title: "Operations"
  description: "Operações de plataforma — observabilidade, alertas, backup, disaster recovery e CI/CD"
  annotations:
    backstage.io/managed-by-location: "url:https://gitlab.staging.internal/platform/backstage-catalog/-/blob/main/entities/domains/catalog-info.yaml"
    backstage.io/techdocs-ref: "dir:."
  tags:
    - operations
    - observability
    - prometheus
    - loki
    - tempo
    - grafana
    - cicd
    - backup
    - sre
spec:
  owner: operations-team

---
# ---------------------------------------------------------------------------
# Domínio 5: shared-services
# Owner: shared-services-team
# Sistemas: vault, kyverno, linkerd, external-secrets, cert-manager, waf
# ---------------------------------------------------------------------------
apiVersion: backstage.io/v1alpha1
kind: Domain
metadata:
  name: shared-services
  title: "Shared Services"
  description: "Serviços compartilhados — secrets management, policy enforcement, mTLS, certificates e WAF"
  annotations:
    backstage.io/managed-by-location: "url:https://gitlab.staging.internal/platform/backstage-catalog/-/blob/main/entities/domains/catalog-info.yaml"
    backstage.io/techdocs-ref: "dir:."
  tags:
    - shared-services
    - vault
    - kyverno
    - linkerd
    - mtls
    - cert-manager
    - external-secrets
    - waf
spec:
  owner: shared-services-team
