# =============================================================================
# secrets.auto.tfvars.example — STAGING
# =============================================================================
#
# ATENÇÃO: Este é apenas um TEMPLATE de documentação.
# NÃO colocar valores reais aqui. Este arquivo É versionado no git.
#
# GAP-TF-006: secrets.auto.tfvars foi descontinuado.
# Use o script: source scripts/export-tf-vars.sh staging
# O script carrega os secrets do AWS Secrets Manager como TF_VAR_*.
#
# Para migration gradual (uso local temporário):
#   1. Copie este arquivo: cp secrets.auto.tfvars.example secrets.auto.tfvars
#   2. Preencha os valores reais (o arquivo é git-ignorado)
#   3. Planeje migrar para o script acima o quanto antes
#
# Variáveis obrigatórias (sem default) — devem ser fornecidas:
# =============================================================================

# --- Vault ---
# Obtido via: vault operator generate-root
# Ou: kubectl get secret vault-root-token -n staging-security-vault -o jsonpath='{.data.root_token}' | base64 -d
vault_root_token = "CHANGE_ME"

# --- Vault OIDC Client ---
# Obtido via Keycloak: Clients > vault > Credentials > Client secret
vault_oidc_client_secret = "CHANGE_ME"

# --- Docker Hub (ECR Pull-Through Cache) ---
# PAT em: https://hub.docker.com/settings/security
# Conta free: 200 pulls/6h autenticado. Credencial em: k8s-platform/staging/dockerhub
docker_hub_username     = "CHANGE_ME"
docker_hub_access_token = "CHANGE_ME"

# --- Grafana ---
# OIDC client secret do Keycloak: Clients > grafana > Credentials
grafana_oidc_client_secret = "CHANGE_ME"
grafana_admin_password     = "CHANGE_ME"

# --- SonarQube ---
sonarqube_postgresql_password = "CHANGE_ME"

# --- Harbor ---
harbor_postgresql_password = "CHANGE_ME"
harbor_admin_password      = "CHANGE_ME"
harbor_redis_password      = "CHANGE_ME"

# --- ArgoCD ---
argocd_postgresql_password = "CHANGE_ME"
argocd_oidc_client_secret  = "CHANGE_ME"

# --- Keycloak ---
keycloak_postgresql_password = "CHANGE_ME"
keycloak_admin_password      = "CHANGE_ME"

# --- Backstage ---
backstage_db_password             = "CHANGE_ME"
backstage_keycloak_client_secret  = "CHANGE_ME"
backstage_gitlab_token            = "CHANGE_ME"
backstage_argocd_token            = "CHANGE_ME"
backstage_sonarqube_token         = "CHANGE_ME"
backstage_auth_session_secret     = "CHANGE_ME"
backstage_harbor_robot_token      = "CHANGE_ME"

# --- Hatch ETL ---
hatch_etl_db_password    = "CHANGE_ME"
hatch_etl_redis_password = "CHANGE_ME"
hatch_etl_api_username   = "CHANGE_ME"
hatch_etl_api_password   = "CHANGE_ME"
